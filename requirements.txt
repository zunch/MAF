# Microsoft Agent Framework - Huvudpaket (Python 3.10+)
# Kräver --pre eftersom det är i preview (från nov 2025)
agent-framework>=1.0.0b251104

# Azure integration (för Azure OpenAI och Azure AI)
azure-identity>=1.15.0

# OpenAI integration (används av agent-framework)
openai>=1.12.0

# Redis för persistent memory (valfritt - aktivera vid behov)
# redis>=5.0.0

# Vektor-databaser för långtidsminne och RAG (valfritt - aktivera vid behov)
# qdrant-client>=1.7.0
# chromadb>=0.4.22

# Data och validering
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Utilities
python-dotenv>=1.0.0

# Logging
structlog>=24.1.0

# Testing (valfritt)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Development tools (valfritt)
black>=24.1.0
ruff>=0.1.0
mypy>=1.8.0
